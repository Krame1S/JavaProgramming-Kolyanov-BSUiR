version: '3'
services:
 app:
    container_name: my_app
    build: .
    environment:
      SPRING_DATASOURCE_URL: jdbc:mariadb://db:3306/db1
    depends_on:
      - db
    ports:
      - "8080:8080"
 db:
    image: mariadb:10.8.2
    environment:
      MARIADB_ROOT_PASSWORD: root
      MYSQL_USER: ilya
      MYSQL_PASSWORD: KOKOWAA
      MYSQL_DATABASE: db1
    volumes:
      - db:/var/lib/mysql
volumes:
 db:
